query FavouritePosts($input: FindFavouritePostsRequest!) {
  favouritePosts(input: $input) {
    data {
      ...PostFragment
    }
    pageInfo {
      afterCursor
      count
      perPage
    }
  }
}
